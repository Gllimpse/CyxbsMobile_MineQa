<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="title"
            type="String" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/shop_item_title_title"
            android:layout_width="wrap_content"
            android:layout_height="28dp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="12dp"
            android:gravity="center"
            android:text="@{title}"
            android:textStyle="bold"
            android:textColor="@color/shop_center_recycler_good_title_good_color"
            android:textSize="20sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/shop_item_title_tip"
            android:layout_width="111dp"
            android:layout_height="17dp"
            android:layout_marginEnd="16dp"
            android:text="@string/shop_center_recycler_title_tip"
            android:textColor="@color/shop_center_recycler_good_title_tip_color"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="@+id/shop_item_title_title"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/shop_item_title_title" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>